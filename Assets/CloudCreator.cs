using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CloudCreator : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
      /*  if(gameObject.transform.scale.x > 1)
        {
            gameObject nextBubble = Instantiate(this.bubble,position,this.transfrom.rotation,this.transform,true);
            nextBubble.transform.scale = new Vector3(gameObject.transform.scale.x,gameObject.transform.scale.x,gameObject.transform.scale.x);
            
        }*/
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
